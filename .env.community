# Community (bring-your-own-key) build. Loaded by `vite build --mode community`.
# The add-in talks to the user's own AI provider; there is no Vaquill backend or
# Supabase project, so no other build args are required.
VITE_EDITION=community
